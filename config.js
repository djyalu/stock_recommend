// 배포 환경 설정
// Vercel 배포 후 여기에 실제 URL을 입력하세요
window.NAVER_PROXY_CONFIG = {
    // Vercel 배포 URL (GitHub Pages에서도 사용)
    vercelProxyUrl: 'https://stock-recommend.vercel.app/api/naver-proxy',
    
    // 또는 환경 변수로 설정 가능
    // vercelProxyUrl: process.env.VERCEL_PROXY_URL || 'https://stock-recommend.vercel.app/api/naver-proxy'
};

